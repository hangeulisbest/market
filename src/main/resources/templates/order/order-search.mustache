{{>layout/header}}
<div class="container-fluid">
    <div class="row">
        <div class="col-mid-12 text-center">
            <h1> 주문 검색</h1>
            <br><br>
            <br>
        </div>
    </div>
    <div class="row">
        <div class="text-center col-md-4 col-md-offset-4">
            <form>
                <div class="form-group">
                    <label for="exampleInputEmail1">유저의 아이디</label>
                    <input type="number" class="form-control" id="userId" placeholder="유저의 아이디를 입력하세요.">
                </div>
                <div class="form-group">
                    <label for="exampleInputPassword1">주문 상태</label>
                    <select class="form-control" id="orderStatus">
                        <option value="ORDER">완료된 주문</option>
                        <option value="CANCEL">취소된 주문</option>
                    </select>
                </div>
                <button type="button" class="btn btn-primary" id="btn-order-search">검색</button>
                <a href="/" role="button" class="btn btn-default">돌아가기</a>
            </form>

        </div>
    </div>
</div>
<br><br>
<table class="table table-horizontal table-bordered">
    <thead class="thead-strong">
    <tr>
        <th>주문 아이디</th>
        <th>주문한 유저의 아이디</th>
        <th>주문한 유저의 이름</th>
        <th>주문한 시간</th>
        <th>배달 상태</th>
        <th>주문 상태</th>
    </tr>
    <tbody id="tbody"></tbody>
</table>

<table class="table table-horizontal table-bordered">
    <thead class="thead-strong">
    <tr id = "paging">
    </tr>
</table>

{{>layout/footer}}